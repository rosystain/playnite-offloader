<UserControl x:Class="Offloader.OffloaderSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="600">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="8">
            <TextBlock Text="{DynamicResource LOCoffloaderSetRemoteRootLabel}" FontWeight="Bold"/>
            <DockPanel Margin="0,4,0,0">
                <Button Content="{DynamicResource LOCoffloaderSetBrowse}" Width="70" DockPanel.Dock="Right" Margin="6,0,0,0" Click="BrowseButton_Click"/>
                <TextBox Text="{Binding Settings.RemoteRoot, UpdateSourceTrigger=PropertyChanged}" VerticalContentAlignment="Center"
                         ToolTip="{DynamicResource LOCoffloaderSetRemoteRootTooltip}"/>
            </DockPanel>

            <TextBlock Text="{DynamicResource LOCoffloaderSetEnrolledTitle}" FontWeight="Bold" Margin="0,12,0,0"/>
            <Button Content="{DynamicResource LOCoffloaderSetManageEnrolled}" Width="150" HorizontalAlignment="Left" Margin="0,4,0,0" Click="OpenEnrolled_Click"/>
        </StackPanel>
    </ScrollViewer>
</UserControl>
