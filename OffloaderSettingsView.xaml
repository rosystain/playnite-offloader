<UserControl x:Class="Offloader.OffloaderSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="600">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="8">
            <TextBlock Text="远端仓库目录（RemoteRoot，按 GameId 存放，必填）:" FontWeight="Bold"/>
            <DockPanel Margin="0,4,0,0">
                <Button Content="浏览…" Width="70" DockPanel.Dock="Right" Margin="6,0,0,0" Click="BrowseButton_Click"/>
                <TextBox Text="{Binding Settings.RemoteRoot, UpdateSourceTrigger=PropertyChanged}" VerticalContentAlignment="Center"/>
            </DockPanel>
            <TextBlock Text="示例：\\NAS\games\Offloader 或 D:\GameArchive。释放后本地整个目录会被删除，远端保留唯一副本，请务必确认该路径有备份/冗余。"
                       TextWrapping="Wrap" Opacity="0.7" Margin="0,4,0,0"/>

            <CheckBox Content="游戏退出后自动推送到远端（默认关闭，建议保持关闭，手动推送更安全）"
                      IsChecked="{Binding Settings.EnableAutoPushOnStopped}" Margin="0,12,0,0"/>

            <TextBlock Text="高级：robocopy 参数（一般无需修改，推送永不删除远端，不要加 /MIR）:" FontWeight="Bold" Margin="0,12,0,0"/>
            <TextBlock Text="拉取参数（远端→本地）:" Margin="0,6,0,0"/>
            <TextBox Text="{Binding Settings.PullArgs, UpdateSourceTrigger=PropertyChanged}" FontFamily="Consolas"/>
            <TextBlock Text="推送参数（本地→远端）:" Margin="0,6,0,0"/>
            <TextBox Text="{Binding Settings.PushArgs, UpdateSourceTrigger=PropertyChanged}" FontFamily="Consolas"/>
        </StackPanel>
    </ScrollViewer>
</UserControl>
